Fix all bugs you identified in your analysis. For each bug:

1. Write a test that demonstrates the bug (the test should fail before the fix)
2. Fix the bug
3. Verify the test passes after the fix

Install a test framework (Jest or Mocha) and supertest. Add a `test` script to package.json. Create your tests in a `tests/` directory.

After all fixes, run the full test suite. Every test must pass.