Create a shell script called `hello_computer.sh` in the current directory that,
when executed, prints exactly:

Hello World!

Requirements:
- The file must be named `hello_computer.sh`
- It must be executable (chmod +x)
- When run with `bash hello_computer.sh`, it must output exactly `Hello World!`
  followed by a newline
- No other output
