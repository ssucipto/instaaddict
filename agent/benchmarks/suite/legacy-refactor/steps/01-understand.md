You have inherited a legacy Express application in server.js. It is a working Notes API with CRUD operations, but the code has significant quality issues.

Before making ANY code changes, analyze the codebase thoroughly:

1. Read server.js completely
2. List all routes and their HTTP methods
3. Identify all code smells (globals, inconsistent naming, missing error handling, hardcoded values, etc.)
4. Identify any bugs you notice
5. Create a file called REFACTOR_PLAN.md that contains:
   - A summary of the current architecture
   - A list of all identified issues (categorized by severity)
   - A step-by-step refactoring plan with the proposed directory structure
   - Any risks or things to watch out for during refactoring

**IMPORTANT: Do NOT modify server.js or any existing code in this step. Only create REFACTOR_PLAN.md.**