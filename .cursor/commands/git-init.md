---
description: "Initialize a git repository with intelligent .gitignore based on project type"
---

# ACP Command: /git-init

Execute ACP Enhanced command `/git-init`.

1. Read and follow **every step** in `agent/commands/git.init.md`.
2. Treat text after the command in the user's message as command arguments.
3. Run the command header from the source file, then continue unless the source explicitly waits for input.

**Canonical source**: `agent/commands/git.init.md`
**Equivalent invocations**: `/git-init`, `@git-init`, `@agent/commands/git.init.md`
